gubuntu -v
ubuntu --v
lsb_release
lsb_release -a
sudo apt-get ubuntu-restricted-extras
sudo apt-get install ubuntu-restricted-extras
sudo apt-get update
sudo apt-get install browser-plugin-fleshplayer-pepperflesh
sudo apt-get install browser-plugin-freshplayer-pepperflesh
sudo apt-get install browser-plugin-freshplayer-pepperfresh
sudo apt-get install browser-plugin-freshplayer-pepperflash
   12  ls
   13  cd Документы/
   14  mkdir Gleb
   15  ls
   16  cd Gleb/
   17  touch text.txt
   18  ls
   19  sudo apt-get install sublime
   20  sudo apt-get install sublime3
   21  sudo apt-get install sublime-text
   22  sudo apt-get install sublime-text-3
   23  sudo apt-get update
   24  sudo apt-get-repository ppa:webupd8team/sublime-text-3
   25  sudo add-apt-repository ppa:webupd8team/sublime-text-3
   26  sudo apt-get update
   27  sudo apt-get install sublime-text-installer
   28  subl text.txt
   29  git init
   30  sudo apt install git
   31  git init
   32  subl text.txt
   33  git add .
   34  git status
   35  git commit -m"initial commit"
   36  git remote add origin https://github.com/genoded/Test.git
   37  git push -u origin master
   38  git remote add origin https://github.com/genoded/Test.git
   39  git push origin master
   40  git status
   41  git commit -m"initial"
   42  git config --global user.email "gkanishchev@gmail.com"
   43  git config --global user.name "gleb"
   44  git commit -m"initial"
   45  git push -u origin master
subl text.txt
git add .
git status
git commit -m "Sosos"
git status
git push 
git checkout -b test
subl text.txt
git add .
git commit
git commit -m "Err"
git checkout master
subl text.txt
git checkout test
   60  subl text.txt
   61  git checkout master
   62  git merge test
   63  git branch -d test
   64  ls
